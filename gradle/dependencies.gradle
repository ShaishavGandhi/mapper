def versions = [
        autoservice: "1.0-rc5",
        kotlin: '1.3.31',
]

def build = [
        javaVersion: JavaVersion.VERSION_1_8,
]

def kotlin = [
        reflect: "org.jetbrains.kotlin:kotlin-reflect:${versions.kotlin}",
        stdlib: "org.jetbrains.kotlin:kotlin-stdlib:${versions.kotlin}",
        stdlibjdk7: "org.jetbrains.kotlin:kotlin-stdlib-jdk7:${versions.kotlin}",
        stdlibjdk8: "org.jetbrains.kotlin:kotlin-stdlib-jdk8:${versions.kotlin}"
]

def apt = [
        autoservice: "com.google.auto.service:auto-service:$versions.autoservice",
        autoCommon: 'com.google.auto:auto-common:0.10',
]

def misc = [
        kotlinxMetdata: "org.jetbrains.kotlinx:kotlinx-metadata-jvm:0.0.6",
        metaData: "me.eugeniomarletti.kotlin.metadata:kotlin-metadata:1.4.0",
        kotlinpoet: 'com.squareup:kotlinpoet:1.2.0'
]

def test = [
        junit: 'junit:junit:4.12',
        truth: 'com.google.truth:truth:0.42'
]

ext.deps = [
        "build": build,
        "apt": apt,
        "versions": versions,
        "kotlin": kotlin,
        "misc": misc,
        "test": test
]
